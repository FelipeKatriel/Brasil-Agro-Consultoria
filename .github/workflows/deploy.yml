name: 🚀 Deploy HTML para GitHub Pages

on:
  push:
    branches: ["main"]  # Dispara quando houver push na branch main
  workflow_dispatch:     # Permite executar manualmente

permissions:
  contents: read         # Permite ler o conteúdo do repositório
  pages: write           # Permite escrever na página do GitHub Pages
  id-token: write        # Necessário para autenticação do Pages

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    name: 📦 Construir e Publicar Site
    runs-on: ubuntu-latest

    steps:
      - name: 🛒 Clonar Repositório
        uses: actions/checkout@v4

      - name: ⚙️ Configurar GitHub Pages
        uses: actions/configure-pages@v5

      - name: 🔍 Verificar existência do index.html
        run: |
          if [ ! -f index.html ]; then
            echo "❌ index

